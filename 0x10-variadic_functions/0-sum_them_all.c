#include <stdio.h>
#include "variadic_functions.h"

/**
 * main - Entry point 
 *
 * @sum: sums all of its parameters.
 * 
 * Return: Always 0.
 */

int sum_them_all(const unsigned int n, ...);
{
	int n = 0;

	printf("%d\n", sum);

	return (0)
}

