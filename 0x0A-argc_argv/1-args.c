#include <stdio.h>

/**
 * main - prints the for the program.
 * @argc: is a couter of argument for int.
 * @argv: is a couter for value of the argument to char
 * Return: Returns always success.
 *
 */

int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", (argc - 1));


	return (0);
}
